const { stdout, stdin } = require("process");
const { createMatrix, loadGrid } = require("./modules/bash-grid.js")

loadGrid([["Hello WOrld", "Test 2"], ["Bob", "Yes we can"]])
// stdout.moveCursor(0, -500)

// while (true)
// {

// }

  